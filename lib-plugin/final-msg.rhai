print();
print("If generated INSIDE a workspace, remember to add this crate to the workspace.");
print();
print("Also, remember to remove any unwanted files (this template currently can't detect this situation, so you need to handle it yourself!)");
print("Possible unwanted files: .gitignore .vscode/*");
print();
